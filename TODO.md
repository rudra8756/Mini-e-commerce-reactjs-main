# TODO: Fix Home.jsx Issues

- [x] Fix useEffect to use API.get("/products") instead of axios.get
- [x] Remove or fix the hero button's onClick (setcart(cart + 1))
- [x] Remove hardcoded product cards from the JSX
- [x] Ensure addToCart function updates cart state correctly
- [x] Verify ToastContainer integration
- [ ] Test product fetching and cart addition
